name: Java CI

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: 24
          distribution: 'temurin'
          cache: 'maven'
      - name: Build & Install
        run: mvn --batch-mode install -DskipTests --no-transfer-progress
      - name: Install Browsers
        run: mvn exec:java --errors -Dexec.mainClass=com.microsoft.playwright.CLI -Dexec.args="install --with-deps"
      - name: Run Tests
        run: mvn test
